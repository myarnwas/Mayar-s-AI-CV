# Optional: backend URL when not using Next.js proxy (default /api is proxied to http://localhost:8000)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Site URL for Open Graph / link preview image (must be absolute). Default: https://personal-ai-cv.onrender.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000
